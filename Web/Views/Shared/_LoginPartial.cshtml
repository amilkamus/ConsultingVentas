@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()
        <ul class="nav navbar-nav">
            @*<li style="margin-right: 20px; visibility:hidden">
                <div class="widget" style="visibility:hidden">
                    <div class="fecha" >
                        <p id="diaSemana" class="horas"></p>
                        <p id="dia" class="dia"></p>
                        <p>de </p>
                        <p id="mes" class="mes"></p>
                        <p>del </p>
                        <p id="anio" class="anio"></p>
                    </div>
                </div>
            </li>
            <li style="margin-right: 20px;">
                <div class="widget" style="visibility:visible">
                    <div class="reloj">
                        <p id="horas" class="horas"></p>
                        <p>:</p>
                        <p id="minutos" class="minutos"></p>
                        <p>:</p>
                        <div class="caja-segundos">
                            <p id="ampm" class="ampm"></p>
                            <p id="segundos" class="segundos"></p>
                        </div>
                    </div>
                </div>
            </li>*@
            <li class="dropdown user user-menu" style="margin-right: 10px;">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <img src="@Url.Content("~/Content/dist/img/user2-160x160.jpg")" class="user-image" alt="User Image">
                    <span> @User.Identity.GetUserName() </span>
                </a>
                <ul class="dropdown-menu">
                    <!-- User image -->
                    <li class="user-header">
                        <img src="@Url.Content("~/Content/dist/img/user2-160x160.jpg")" class="img-circle" alt="User Image">
                        <p>
                            @User.Identity.GetUserName()
                        </p>
                        @if (User.IsInRole("ADMINISTRADOR"))
                        {
                            <p> ADMINISTRADOR </p>
                        }
                        else if (User.IsInRole("OPERADOR"))
                        {
                            <p> OPERADOR </p>
                        }
                        else if (User.IsInRole("CONSULTOR"))
                        {
                            <p> CONSULTOR </p>
                        }
                    </li>
                    <!-- Menu Footer-->
                    <li class="user-footer">
                        @*<div class="pull-left">
                                <a href="#" class="btn btn-default btn-flat"><i class="fa fa-key"></i> Cambiar Contraseña</a>
                            </div>*@
                        <div style="text-align:center">
                            <a href="javascript:document.getElementById('logoutForm').submit()" class="btn btn-default btn-flat"><i class="fa fa-sign-out"></i> Cerrar Sesion</a>
                        </div>
                    </li>
                </ul>
            </li>
        </ul>
    }
}
else
{
    <ul class="nav navbar-nav navbar-right">
        <li>
            <a href="@Url.Action("Login", "Account")"></a>
        </li>
    </ul>
}